# Temporary Next-Steps Handoff

> Overwrite this file before each new code-writing batch.

## Checkpoint

Clean/pushed at `0c0e845`. Branching, V4 plan, and full Friday canonicalization are complete. V4 capture remains the only interactive blocker.

## Current batch: refresh standalone senior report

Update `scripts/17_build_rssm_report.py` and regenerate the standalone HTML to include:

1. validation-only ordinary-draw branching contract;
2. explicit outcome-conditioned branch architecture/results and caveats;
3. active-versus-quiet LM-branch evidence;
4. V4 frozen checkpoint/plan and the fact that capture has not started;
5. full Friday PCAPNG adapter counts/runtime/memory and label-free limitation;
6. updated next steps emphasizing fresh V4 evidence rather than more V3 tuning.

Do not alter or rerun V3 test predictions. Copy the one-file report to `/home/paprika/Downloads/rssm_eod_report.html`, verify no external resources, record SHA-256, then commit/push the generator/docs handoff.
